module.exports = class AddressValidator{
    validateAddress(address) {
        let errors;
        if(address.addressLine === "1"){
            errors = "test"
        }
        return "test";
    }
}
